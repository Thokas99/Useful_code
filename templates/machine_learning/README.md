# Machine-learning templates

The active machine-learning library is organized by analytical purpose. The
current family is [`biomarkers/`](biomarkers/), which contains a sample-level
binary classifier and a reporting notebook.

Both templates are `SOURCE-BACKED WORKFLOW` and currently `draft`. They use
training-only preprocessing, a locked test set, and explicit TSV handoffs.
They are not clinical prediction pipelines.
